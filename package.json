{
  "name": "prj-clean-node-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "test": "jest --watchAll --skip-no-tests",
    "test:staged": "jest",
    "postinstall": "husky install"
  },
  "author": "@ifoxxiedev",
  "devDependencies": {
    "husky": "^8.0.0",
    "jest": "^29.1.2",
    "lint-staged": "^13.0.3",
    "standard": "^17.0.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "lint-staged": {
    "*.js": [
      "standard --fix",
      "yarn test:staged",
      "git add ."
    ]
  }
}
